1. Stack:
	 stack<TreeNode*> stack;
	 stack.top() //check top value
	 stack.pop() //pop top value
	 stack.push(a) //push value

2. priority_queue
	- in c++, default is decreasing order(max heap)
	- if need inc seq, use greater<int>
	- constructor:
		- priority_queue<int> c1	//without amp
		- priority_queue<int, vector<int>, decltype(cmp)> pq(cml)			//self cmp func
	- pq.push(<element>)
	- pq.top	// same as peek
	- pq.pop	// no ret value, just delete top
	- reverse order:       greater<int> substu cml
  
3. Deque
	- push_front(), push_back()
  
3.queue
	- q.push
  - q.pop
  - q.top

5. Vector
	- initialize with same value:    vector<int> a(10,0)	//initialize all 10 values with 0
	- sort(v.begin(), v.end())
	- sort(v.begin(),v.end(),greater<int>())  //reverse
  
6. unordered_map
	- add new value:      mp.insert({a, b});
	- update value: mp[a] = <new value>
	- check if key exists:     mp.find(val) == mp.end()?
	- retrieve value: 	val = mp[key]

7. Map
	- delete key: 	map.erase(<val>)
	- floor key:	map.lower_bound(val)
	- upper key:	map.upper_bound(val) //ret item
	- loop through map:	map<int,int>::iterator it;
					it = mp.begin();it != mp.end();it++
	- 
8. Pair
	- initialize: pair<int,int> p(1, 2);   
			make_pair(1, 2);
	- access:  p.first, p.second

9. Set
	- can directly use pair as the key. unordered_set cannot use it as the key
  
  
